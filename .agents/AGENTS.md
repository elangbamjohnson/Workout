
- Do NOT commit or push code using git unless the user explicitly asks you to do so.

- Cross-Platform Compatibility: When changing or fixing UI/UX elements, ALWAYS consider cross-platform compatibility ensuring that it works correctly on iOS, Android, and Web browsers.

- Reuse Existing Exercise Videos: Whenever adding or updating exercises or warm-up drills, always check if a video demo for the same movement already exists elsewhere in the codebase (across other days or quick sessions). Always reuse the existing `videoId` and `videoFormat` to maintain consistency across the entire app, even if a new video ID or link is provided in a prompt.
